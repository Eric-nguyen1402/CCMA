# @package _global_
defaults:
  - override /init_strategy: random_init

query_strategy:
  name: probcover
  params:
    batch_size: 1024 #10240 original 
    delta_iter: 6  # number of binary search ops to find delta
    delta: null
